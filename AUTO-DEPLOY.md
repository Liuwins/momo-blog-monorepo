# 部署文档入口

本文档已收敛为兼容入口，不再包含可直接执行的旧部署脚本。

请使用根目录的 [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)。它是当前 Monorepo 唯一的部署基线，覆盖 Docker Compose、非 Docker、Nginx、端口、上传限制、迁移、备份和验证。

旧版本部署手册仍可从 Git 历史查看，但其中的域名、路径、端口和凭据假设未经当前环境核对。
