# MomoBlog 当前路线图

状态：2026-08-21，根据当前源码更新。

## 已实现

- 单管理员 JWT 登录，无注册入口；
- 动态发布、编辑、删除，支持文本、Markdown、图片、视频、标签和配乐字段；
- 游客/登录用户评论、回复、审核、删除；
- 登录用户和 visitorId 点赞；
- 关注关系与关注流；
- 通知列表、未读数和 Socket.IO 实时推送；
- 个人资料、头像、封面、背景音乐和暗黑主题；
- 本地收藏、浏览历史、草稿、天气和每日一句；
- 图片多尺寸处理、视频/音频上传、Open Graph 分享页；
- Docker Compose、Nginx、SQLite 迁移和备份脚本。

## 优先待办

### 高优先级

1. 修复动态配乐端到端持久化：DTO、`PostsService.create/update/find`、个人动态查询和回归测试需要统一确认。
2. 补充核心自动化测试：鉴权、动态、评论审核、上传限制、迁移和前端路由守卫。
3. 完成真实部署基线核对：确认域名、证书、Nginx、备份目录、OSS、容器端口和恢复演练。

### 中优先级

1. 为 `cleanup-images.sh` 增加视频、配乐、背景图和背景音乐引用扫描；当前脚本默认只报告，确认后才允许删除。
2. 将 Docker Compose 后端端口改为本机绑定或内部网络，避免绕过前端 Nginx 直接暴露 API。
3. 增加 CI：前端 lint/test/build、后端 typecheck/build、敏感文件扫描。
4. 改善移动端评论输入框在软键盘弹出时的可见性。

### 低优先级

1. PWA 离线能力；
2. 动态编辑历史和回滚；
3. 更细粒度的评论回复通知；
4. CSP nonce 和更严格的媒体内容校验。

## 开发规则

- 前后端跨端功能使用一个 Monorepo 提交，提交信息说明影响范围。
- 修改实体必须补迁移；不要用 seed 代替迁移。
- 修改上传、通知或代理时，同时更新 `docs/DEPLOYMENT.md` 和 `PROJECT-WIKI.md`。
- 保持 SQLite 单实例写入；不要直接扩展 PM2 instances。
- 不提交 `.env`、数据库、上传文件、日志、构建产物和截图。
