name: CI

on:
  push:
    branches: [master, main]
  pull_request:

permissions:
  contents: read

jobs:
  repository-hygiene:
    name: Repository hygiene
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Reject sensitive or generated files
        run: node scripts/repository-hygiene.mjs
      - name: Validate Compose interpolation
        env:
          JWT_SECRET: ci-only-not-a-production-secret
          CLIENT_ORIGIN: http://localhost
          SITE_URL: http://localhost
        run: docker compose -f momo-blog-server/docker-compose.yml config --quiet
      - name: Validate operations scripts
        run: bash -n momo-blog-server/backup.sh momo-blog-server/cleanup-images.sh momo-blog-server/restore-media-quarantine.sh momo-blog-server/cert-check.sh momo-blog-server/scripts/production-smoke.sh

  frontend:
    name: Frontend checks
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: momo-blog
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: momo-blog/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Unit tests
        run: npm test
      - name: Production build
        run: npm run build
      - name: Dependency audit
        run: npm audit --audit-level=high --registry=https://registry.npmjs.org

  backend:
    name: Backend checks
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: momo-blog-server
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: momo-blog-server/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Unit tests
        run: npm test
      - name: Typecheck
        run: npm run typecheck
      - name: Production build
        run: npm run build
      - name: Production dependency audit
        run: npm audit --omit=dev --audit-level=high --registry=https://registry.npmjs.org
